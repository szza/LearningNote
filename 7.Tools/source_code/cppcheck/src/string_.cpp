#include<string>
#include<iostream>
#include<stdio.h>


int main(int argc, char const *argv[]) {
    // std::string str = "szz";
    // char age[12];
    // snprintf(age, sizeof(age), "%2d", 25);
    // std::cout<<str + age<<std::endl;
    char* pt = new char;
    return 0;
}
